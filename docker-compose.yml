services:
  app:
    build: .
    expose:
      - "8080"
    volumes:
      - rental-data:/app/data
    environment:
      - TZ=Asia/Tokyo
      - FETCH_INTERVAL_HOURS=3
      - FETCH_MAX_PAGES=10
      - FETCH_WARD_CODES=13104,13113,13114
      - MAX_AGE_DAYS=30
      - DB_PATH=/app/data/rental.db
    restart: unless-stopped
    labels:
      - traefik.enable=true
      - traefik.http.routers.rental-http.rule=Host(`7days.actraise.org`)
      - traefik.http.routers.rental-http.entrypoints=http
      - traefik.http.routers.rental-http.middlewares=redirect-to-https
      - traefik.http.routers.rental-https.rule=Host(`7days.actraise.org`)
      - traefik.http.routers.rental-https.entrypoints=https
      - traefik.http.routers.rental-https.tls=true
      - traefik.http.routers.rental-https.tls.certresolver=letsencrypt
      - traefik.http.routers.rental-https.middlewares=gzip
      - traefik.http.services.rental.loadbalancer.server.port=8080
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s

volumes:
  rental-data:
